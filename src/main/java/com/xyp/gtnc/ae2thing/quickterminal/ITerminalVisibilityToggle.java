package com.xyp.gtnc.ae2thing.quickterminal;

/** Implemented by non-AE2 interface-terminal entries that expose a persistent visibility switch. */
public interface ITerminalVisibilityToggle {

    void toggleTerminalVisibility();
}
